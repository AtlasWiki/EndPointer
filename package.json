{
  "name": "placeholder-extension",
  "version": "0.1",
  "description": "Chrome/Firefox extension for DOM and JS logging/parsing.",
  "main": "webpack.config.js",
  "scripts": {
    "build:chrome": "webpack --env BROWSER=chrome && rimraf temp-chrome-prod",
    "build:firefox": "webpack --env BROWSER=firefox && rimraf temp-firefox-prod",
    "build": "npm run build:chrome && npm run build:firefox"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.4",
    "webpack": "^5.93.0",
    "webpack-cli": "^5.1.4",
    "zip-webpack-plugin": "^4.0.1"
  },
  "dependencies": {
    "rimraf": "^6.0.1"
  }
}
